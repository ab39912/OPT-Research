# pure_python_stats.py and cross_dataset.py need NONE of these (stdlib only).
# Required only for the pandas / polars / bonus scripts.
pandas>=2.0
polars>=0.20
matplotlib>=3.7   # only for visualize.py (bonus), if you add one
